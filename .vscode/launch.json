{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug MattSQL",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/debug/mattsql",
      "args": ["SELECT 1 AS one, 'MattSQL' AS project;"],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "CMake: build debug",
      "terminal": "integrated"
    },
    {
      "name": "Debug MattSQL Tests",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/debug/mattsql_tests",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "CMake: build debug",
      "terminal": "integrated"
    }
  ]
}
